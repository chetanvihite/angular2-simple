# angular2-simple

Super simple Angular 2 App with TypeScript and SystemJS

1. `npm install -g typescript http-server tsd`

1. `npm install`

1. `tsd reinstall -r -o`

1. `tsc -p ./src/`

1. `http-server`